# Handoff — Incident Radar visual redesign

## Completed
- Implemented the Full dashboard visual redesign from
  `design_handoff_incident_radar/README.md` and `Incident Radar.html`.
- Commit: `1b6d68f` on branch `claude/quirky-sutherland-bee0c8`, pushed.

### Files changed
- `app.py` — replaced global CSS with design tokens (light + dark), Geist
  + Geist Mono load, new topbar/footer, 4-column service grid, theme
  radio in sidebar.
- `components.py` — full rewrite of `render_top_metrics`,
  `render_known_issues`, `render_service_card`, `render_detail_panel`,
  `render_signal_table`. Added helpers `_badge`, `_caption`, `_spark`,
  `_ribbon`, `_ticket_note_html`. `render_timeline` kept as a no-op
  (timeline now drawn inside the detail panel). Compact-widget functions
  untouched in structure; only hex colors swapped for CSS vars.
- `mock_data.py` — muted `STATUS_COLORS`, added `generate_status_ribbon`
  (24 5-min ticks) and `generate_traffic_series` (11-point sparkline
  series), enriched `generate_known_issues` with `evidence`,
  `started_utc`, `status_key`, `service`; added per-status counts to
  `generate_summary_metrics`.
- `scenarios.py` — attaches `ribbon` and `traffic_series` to each
  service when loading.

## Uncommitted
None.

## Verification
- `pytest tests` — 2 passed.
- `python -m py_compile` on all four modules — clean.
- Data-layer smoke against the `Printing / document generation` scenario
  produces the expected ribbon `[14×green, 4×orange, 6×red]` and
  `overall_status == "red"`.
- The Streamlit app was not started in this session (no working
  streamlit install in the sandbox); the visual fidelity should be
  verified by running `streamlit run app.py` and comparing
  side-by-side with `design_handoff_incident_radar/Incident Radar.html`
  under the Printing/Technical scenario.

## Known issues / follow-ups
- Theme is toggled via a sidebar radio (Option B from the handoff) — it
  rerenders on switch. If the team prefers Option A (`config.toml`),
  swap the radio for `st.get_option("theme.base")`.
- `render_timeline` is now a stub (timeline ships inside
  `render_detail_panel`). The unused import was already removed from
  `app.py`.
- The detail-panel action buttons (`Open ticket`, `Subscribe`,
  `Copy ticket note`) are non-functional placeholders, matching the
  static reference.

## Suggested next task
- Run the app under each scenario and tune any spacing/font fallbacks
  that drift from the reference HTML.
- If desired, restore the `numpy` import only if performance becomes a
  concern — the current series generators use the stdlib RNG via
  `_seeded_random`.
